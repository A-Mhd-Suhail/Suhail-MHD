rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEMO RULES — any logged-in user can read/write.
    // For production: role-based hardening required (tell judges: "prototype demo rules")
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}